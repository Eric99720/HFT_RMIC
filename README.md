# HFT_RMIC

<!-- project-state:begin -->
<!-- Generated by scripts/sync_project_records.py; edit docs/project_state.json. -->
Record revision: `2026-09-18-i4-functional-ready-v1`.

**I4 / VERIFYING** — I4 focused R01 integration is functionally assembled: ordinary bridge and R01-prebuild order producers share one fail-closed futures risk path, committed execution and CL admission use one transaction-locked futures-state/AMU owner, and CI verifies closed-loop fill-to-close behavior, execution priority, source priority and reject suppression. Frozen-encoder 80-byte XSim parity and real-AMU U50 OOC are the remaining I4 gates.

Status observed: `2026-09-17T18:09:00Z`; this is a recorded handoff, not live telemetry.

Goal: Integrate the frozen dual-XGMII HFT baseline with the frozen RMIC M5.4 transactional core without modifying either upstream, while closing TAIFEX futures accounting, execution idempotency, safety-policy, timing, and end-to-end validation gaps.

Next: Run the prepared I4 frozen-encoder byte-parity XSim and real-AMU U50 R01-path OOC implementation, review their evidence, then close PR #3 before full dual-XGMII integration.

[Active plan](docs/exec_plans/2026-09-18-i4-r01-risk-integration.md) · [Task board](TASKS.md) · [Progress](PROGRESS.md).
<!-- project-state:end -->

Integration workspace for the frozen `hft-full-system-fpga` HFT baseline and frozen `RMIC` M5.4 risk-management core.

## Repository contract

This is the only repository where HFT/RMIC integration glue, adapters, futures accounting, policy logic, integration tops and cross-project verification are developed. The upstream repositories remain pinned read-only submodules.

- HFT: `50217fad1fd580f8c451ba893f9035f4be1dc21a`
- RMIC: `de6c300f4f18b18296f013287ab0eca70d3abb72`

## Closed hardware evidence

I2 futures state/execution OOC: routed WNS `+1.078 ns`, TNS `0`, 3396 LUT, 2383 FF, 19 RAMB36 + 1 RAMB18, 10 DSP.

I3 atomic CL2EX gate OOC: routed WNS `+1.583 ns`, TNS `0`, 2392 LUT, 2693 FF, 11 RAMB36 + 1 RAMB18, 10 DSP. Its worst path remains in the frozen AMU.

See `docs/results/` and `DECISIONS.md` for scope and claim boundaries.

## I4 verifying boundary

I4 adds one shared futures-state/AMU owner for CL admission plus committed execution, merges the ordinary bridge and R01-prebuild order sources ahead of risk, and feeds only accepted original payloads into the pinned frozen `financial_protocol_encoder`.

Prepared local sign-off:

```powershell
pwsh .\scripts\run_i4_r01_parity_xsim.ps1
pwsh .\scripts\run_i4_r01_path_ooc_impl.ps1
```

The first command requires full 80-byte parity against a direct frozen-encoder baseline. The second requires the real pinned XPM AMU plus frozen R01 encoder to close on U50 at 6.400 ns.

## Setup

```powershell
git submodule update --init --recursive
pwsh .\scripts\setup.ps1
pwsh .\scripts\preflight.ps1
```

Target: Vivado 2022.1, U50 `xcu50-fsvh2104-2-e`, 156.25 MHz / 6.400 ns.

## Project workflow

Start with `CURRENT_PHASE.md`, `docs/README.md`, the active ExecPlan and `AGENTS.md`. `docs/project_state.json` is canonical. Feature/research work uses one `codex/<phase-id>` branch and one PR through phase closure.

## Evidence policy

Simulation is not board evidence; routed timing is not packet latency; vectorless power is not measured board power. Futures margin v1 remains a host-configured research risk-budget model, not TAIFEX SPAN.
