# HFT_RMIC

<!-- project-state:begin -->
<!-- Generated by scripts/sync_project_records.py; edit docs/project_state.json. -->
Record revision: `2026-09-17-i2-governance-v1`.

**I2 / RUNNING** — I1 field/policy adapters, futures accounting v1, and R02/R32 PositionEffect tap pass CI. Project governance is being formalized and I2 now owns multi-account/product futures state plus committed execution integration.

Status observed: `2026-09-17T10:28:00Z`; this is a recorded handoff, not live telemetry.

Goal: Integrate the frozen dual-XGMII HFT baseline with the frozen RMIC M5.4 transactional core without modifying either upstream, while closing TAIFEX futures accounting, execution idempotency, safety-policy, timing, and end-to-end validation gaps.

Next: Implement and verify the I2 multi-account/multi-product futures state manager, then bind only committed/de-duplicated R02/R32 execution events to state transitions.

[Active plan](docs/exec_plans/2026-09-17-i2-futures-state-manager.md) · [Task board](TASKS.md) · [Progress](PROGRESS.md).
<!-- project-state:end -->

Integration workspace for the frozen `hft-full-system-fpga` HFT baseline and frozen `RMIC` M5.4 risk-management core.

## Repository contract

This is the **only** repository where HFT/RMIC integration glue, adapters, futures accounting, policy logic, integration tops and cross-project verification are developed.

Pinned read-only upstreams:

- HFT: `Eric99720/hft-full-system-fpga` @ `50217fad1fd580f8c451ba893f9035f4be1dc21a`
- RMIC: `Eric99720/RMIC` @ `de6c300f4f18b18296f013287ab0eca70d3abb72`

Do not modify source inside `deps/`. A pin change is a separate migration decision and phase.

## Architecture

```text
market-data XGMII RX
  -> frozen HFT RX/CDC/order book/strategy
  -> frozen HFT 256-bit order_data
  -> HFT_RMIC field/account/product adapters
  -> HFT_RMIC fail-closed policy layer
  -> HFT_RMIC futures accounting/state
  -> frozen RMIC transactional/order-lifecycle core
  -> PASS: original HFT order payload
  -> frozen TMP R01 encoder / TCP / XGMII TX

TAIFEX R02/R32
  -> frozen HFT TMP decoder
  -> frozen HFT report-sequence owner (commit/dedup/gap handling)
  -> HFT_RMIC PositionEffect/exec adapter
  -> futures accounting + RMIC EX2CL reconciliation
```

RMIC does **not** replace the network layer, TMP session, decoder, order book, strategy, encoder, TCP/IP stack or PCS/PMA path.

## Current technical baseline

### Frozen HFT

The pinned HFT source is the frozen pre-hardware dual-XGMII baseline with market/trading ports, RX-HOT, CDC, order book/strategy, R01 fast path, ARP/TCP/TMP lifecycle/replay/flow-control and the accepted simulation-derived 83-cycle / 531.2 ns Gao-style nominal baseline. It is not board/live-exchange sign-off.

### Frozen RMIC

The pinned RMIC source is the M5.4 dual-flow baseline with 4096-entry collision-capable AMU, conflict-free CL2EX II=1, concurrent unrelated CL2EX/EX2CL, account/order hazards, hardware fill/cancel/reject reconciliation and U50 156.25 MHz post-route timing closure.

### Integration-owned additions

Already validated by CI:

- exact account/product mapping and side conversion;
- fail-closed policy shell and extensible reject namespace;
- TAIFEX TMP quantity/Side/OrdType/TIF/PositionEffect contract constants;
- futures accounting v1 with separate long/short and OPEN/CLOSE reservations;
- R02/R32 PositionEffect side-band tap;
- dependency/interface drift checks.

The v1 margin model is host-configured `margin_per_contract × gross open exposure`; it is a research risk-budget model, **not** a TAIFEX SPAN claim.

## First-time setup (Windows PowerShell 7)

```powershell
git clone --recurse-submodules https://github.com/Eric99720/HFT_RMIC.git
cd HFT_RMIC
pwsh .\scripts\setup.ps1
pwsh .\scripts\preflight.ps1
```

Existing checkout:

```powershell
git pull
git submodule update --init --recursive
pwsh .\scripts\setup.ps1
pwsh .\scripts\preflight.ps1
```

Expected dependency identities:

```text
deps\hft-full-system-fpga=50217fad1fd580f8c451ba893f9035f4be1dc21a
deps\RMIC=de6c300f4f18b18296f013287ab0eca70d3abb72
```

## Project workflow

Start with:

1. [`CURRENT_PHASE.md`](CURRENT_PHASE.md)
2. [`docs/README.md`](docs/README.md)
3. active ExecPlan linked above
4. [`AGENTS.md`](AGENTS.md) for repository-wide agent rules

Material state is authored in `docs/project_state.json` and synchronized into the root records:

```powershell
python -B scripts/sync_project_records.py --write
python -B scripts/sync_project_records.py --check
python -B scripts/check_repository_layout.py
```

Feature/research work uses one `codex/<phase-id>` branch and one PR through phase closure. Do not feature-commit directly to `main`.

## Verification entry points

```powershell
pwsh .\scripts\setup.ps1
pwsh .\scripts\preflight.ps1
```

Individual regression entry points:

```text
bash scripts/run_adapter_iverilog.sh
bash scripts/run_accounting_iverilog.sh
```

Vivado sign-off target unless superseded by a durable decision:

```text
Vivado 2022.1
xcu50-fsvh2104-2-e
156.25 MHz / 6.400 ns
```

## Evidence policy

Claims are separated into source/spec, functional simulation, synthesis/OOC, post-route and hardware/live-system evidence. Simulation latency is not board latency; routed timing is not physical packet latency; vectorless power is not measured board power. See [`docs/results_policy.md`](docs/results_policy.md).

No exchange-specific rule is called compliant until validated against the named authoritative TAIFEX version and the required integration evidence.

## Repository layout

```text
deps/              frozen read-only HFT/RMIC submodules
rtl/adapters/      HFT/TMP <-> normalized RM adapters
rtl/policy/        fail-closed configurable risk policy
rtl/accounting/    futures accounting/state transition logic
rtl/integration/   full-system composition owned by this repo
rtl/include/       integration contracts/reason/protocol constants
tb/                self-checking integration-owned testbenches
scripts/           setup, preflight, regression and repository checks
docs/              architecture/contracts/project records/exec plans
build/ reports/    local generated output; ignored
```

See [`docs/repository_layout.md`](docs/repository_layout.md) for ownership rules.

## Research direction

The frozen M5.4 RMIC is treated as architecturally complete for generic transactional risk/order lifecycle. TAIFEX futures completeness is an integration responsibility: OPEN/CLOSE and long/short accounting, margin policy, committed execution idempotency, readiness/recovery, rate/outstanding limits, end-to-end byte correctness, timing/resource A/B comparison and eventual hardware validation.
