# Task Board

Current task state is generated from `docs/project_state.json`. Acceptance criteria belong with the task; detailed implementation/evidence belongs in the active ExecPlan.

<!-- project-state:begin -->
<!-- Generated by scripts/sync_project_records.py; edit docs/project_state.json. -->
Record revision: `2026-09-17-i2-functional-closed-v1`.

| ID | Status | Task | Acceptance |
| --- | --- | --- | --- |
| `I2-01` | **COMPLETE** | Adopt repository governance and phase workflow | Canonical project records, AGENTS policy, layout/results policies, sync/layout checks and CI governance checks are present and passing. |
| `I2-02` | **COMPLETE** | Implement multi-account/multi-product futures state manager | Independent account/product state, OPEN/CLOSE long/short reservations and fills are self-checking with no cross-account contamination and fail-closed invalid access. |
| `I2-03` | **COMPLETE** | Bind committed TAIFEX execution/reject lifecycle | Order context resolves account/product/PositionEffect by order_id; committed R02/R32 fill, terminal release, cancel and reduce plus validated R03 reject release are self-checking; stale/duplicate/mismatched events cannot double-apply state. |
| `I2-04` | **VERIFYING** | Measure I2 real-AMU OOC timing/resources | Vivado 2022.1 OOC composition using the pinned RMIC XPM AMU at 6.400 ns reports real block RAM use, WNS >= 0, TNS = 0, zero routing errors and reviewed utilization/DRC/power evidence. |
| `I3-01` | **PLANNED** | Build CL2EX futures risk gate and atomic reservation/order-context admission | Risk-rejected orders never enter R01; accepted original HFT payload is byte-identical; futures reserve plus order-context insertion is atomic or deterministically rolled back. |

<!-- project-state:end -->

Historical completed work is retained in `PROGRESS.md` and Git history rather than copied into the current task board.
